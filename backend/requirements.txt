# HeartLens backend — PINNED versions (see CLAUDE.md §4)
# Version mismatches against the pickled pipelines can SILENTLY corrupt predictions.

# --- ML / model pipeline (must match the versions the .pkl files were trained under) ---
scikit-learn==1.6.1
# Pinned to the version the pipelines were pickled under (per models/model_metadata.json).
xgboost==3.2.0
joblib>=1.3
pandas>=2.0

# --- Web framework ---
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9          # required by FastAPI for UploadFile (image upload)

# --- OCR (heavy: pulls in PyTorch; prefer CPU-only +cpu wheels in deployment, see §9) ---
# EasyOCR is initialized for BOTH 'en' and 'bn' and built ONCE, lazily (see ocr.py, §8).
# For Railway, install CPU-only torch to cut image size/memory (§9), e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
easyocr>=1.7

# --- Config ---
python-dotenv>=1.0
pydantic>=2.6

# --- DB (add when wired up) ---
# psycopg[binary]>=3.1
# sqlalchemy>=2.0
